spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test?useSSL=false&useUnicode=true&autoReconnect=true&jdbcCompliantTruncation=false
    username: root
    password: root

logging:
  level:
    demo:
      dal:
        dao:
          mapper: TRACE